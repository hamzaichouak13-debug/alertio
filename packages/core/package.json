{
  "name": "@alertio/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core matching engine, ROME tree, types — shared across web, mobile and scheduler",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build":    "tsc",
    "test":     "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
